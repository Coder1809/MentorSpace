{
  "name": "mentorspace",
  "type": "module",
  "version": "1.0.0",
  "description": "MentorSpace Mentorship Platform",
  "main": "server.js",
  "scripts": {
    "dev:server": "nodemon server/server.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build:client": "cd client && npm --force install && npm run build",
    "build:server": "npm install",
    "build": "npm run build:client && npm run build:server",
    "seed": "node server/seed.js",
    "test": "node server/test_api.js",
    "start": "cross-env NODE_ENV=production node server/server.js"
  },
  "author": "Rushil Reddy",
  "license": "ISC",
  "dependencies": {
    "accesscontrol": "^2.2.1",
    "axios": "^1.10.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.14.1",
    "razorpay": "^2.9.6"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0",
    "nodemon": "^3.1.10"
  }
}
